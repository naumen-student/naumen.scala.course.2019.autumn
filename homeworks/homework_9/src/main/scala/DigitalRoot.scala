
/**
  * Цифровым корнем называется число, равное рекурсивной сумме всех цифр исходного числа.
  * findRoot(99)
  *   = findRoot(9 + 9)
  *   = findRoot(18)
  *   = findRoot(9)
  *   = 9
  */
trait DigitalRoot {

    /**
      * Функция не должна содержать переменных и циклов
      */
    def findRoot(n: Int): Int = ???
}
